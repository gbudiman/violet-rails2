dist: trusty
language: ruby
rvm:
  - 2.5.3
services:
  - postgresql
addons:
  postgresql: "10.5"
script:
  - bundle exec rake db:reset db:setup db:migrate RAILS_ENV=test
  - bundle exec rspec
  - bundle exec rubocop
